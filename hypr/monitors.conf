# ============================================================
# Monitor Configuration
# Omarchy default: auto-detect all monitors at preferred resolution
# Edit this if you need a specific resolution, refresh rate, or scale.
#
# Format: monitor = name, resolution@hz, position, scale
# Examples:
#   monitor = eDP-1, 2560x1600@120, 0x0, 1.6       # HiDPI laptop
#   monitor = HDMI-A-1, 1920x1080@60, 2560x0, 1    # External monitor
#   monitor = , preferred, auto, 1                  # Auto (all monitors)
# ============================================================

# Auto-detect: works for most single and dual monitor setups
monitor = , preferred, auto, 1
